auto-install-peers=true
strict-peer-dependencies=false
# Плоский node_modules — требуется для Expo/React Native (Metro плохо дружит
# со строгой симлинк-раскладкой pnpm). Затрагивает весь воркспейс.
node-linker=hoisted
